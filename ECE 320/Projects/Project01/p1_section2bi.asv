%--------------------------------------------------------------------------
% 2.c:
%--------------------------------------------------------------------------
nh = 0:7;
h = (3.7).^nh;
x = ones(1,181);
L = 10;
y = oafilt(h,x,L);
ny = 0:length(y)-1;
stem(ny,y);
title('h[n]=(3.7)^n(u[n]-u[n-8]) with L=10');xlabel('time');ylabel('y[n]');
